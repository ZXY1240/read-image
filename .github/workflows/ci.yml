name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  quality:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: astral-sh/setup-uv@22695119d769bdb6f7032ad67b9bca0ef8c4a174 # v5.4.0
        with:
          version: "latest"
          python-version: ${{ matrix.python-version }}
      - name: Install dependencies
        run: uv sync --extra dev
      - name: Install Playwright Chromium
        run: uv run playwright install --with-deps chromium
      - name: Lint
        run: uv run ruff check .
      - name: Format check
        run: uv run ruff format --check .
      - name: Type check
        run: uv run mypy omnimodal
      - name: Test
        env:
          RUN_BROWSER_INTEGRATION: "1"
        run: uv run pytest
      - name: Validate plugin
        run: uv run python scripts/validate_plugin.py
      - name: Prepare public release copy
        run: uv run python scripts/prepare_public_release.py --output "$RUNNER_TEMP/read-image-public"
      - name: Validate public release copy
        run: uv run python scripts/validate_plugin.py --root "$RUNNER_TEMP/read-image-public" --public

  windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
      - uses: astral-sh/setup-uv@22695119d769bdb6f7032ad67b9bca0ef8c4a174 # v5.4.0
        with:
          version: "latest"
      - name: Install dependencies
        run: uv sync --extra dev
      - name: PowerShell syntax check
        shell: pwsh
        run: |
          $scripts = Get-ChildItem omnimodal/assets/windows/*.ps1
          foreach ($script in $scripts) {
            [scriptblock]::Create((Get-Content $script.FullName -Raw)) | Out-Null
          }
      - name: Tests
        env:
          # GitHub Actions runners do not provide a stable interactive desktop;
          # real window capture is covered by the local integration test.
          RUN_WINDOWS_CAPTURE_INTEGRATION: "0"
        run: uv run pytest
